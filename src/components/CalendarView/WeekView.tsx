import { EventType, SubViewType } from '../../types/types'

const WeekView = ({subViewList, event}: { subViewList: SubViewType[], event: EventType }) => {
  return (
    <div>
      WEEK VIEW
    </div>
  )
}

export default WeekView
