import { EventType, SubViewType } from '../../types/types'

const MonthView = ({subViewList, event}: { subViewList: SubViewType[], event: EventType }) => {
  return (
    <div>
      MONTH VIEW
    </div>
  )
}

export default MonthView